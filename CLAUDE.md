# Camp — 運用ルール

要件・仕様・意思決定ログの一次情報源は Obsidian Vault 側 `Human/Projects/Camp.md`（決定の経緯・背景もそちら）。ここには、このリポジトリで作業するセッションが実装時に必ず守る振る舞いルールだけを置く。

## レビューの受け取り方（2026-09-02決定）

このプロジェクトを駆動している本人は、レビュー（実装エージェント自身によるものを含む）が指摘する内容を、実装と同レベルの技術検証能力では確認できない。したがって、実装・レビューを行う側は指摘をそのまま投げず、次の形に翻訳して報告すること。

各指摘に次の4点を必ず添える：

1. **何が起きるか**（技術的な事実）
2. **証拠**（実測・再現条件・具体的な数値。無ければ「未検証」と明記する。「〜のはず」で終わらせない）
3. **間違っていた場合の被害規模と、取り返しがつくか**（不可逆性）
4. **本人が今決めるべきことがあるか**（無ければ「決定不要、実装側で処理する」と明記）

本人の判断は技術的な真偽検証ではなく、3と4に基づくリスク・優先度判断に絞る。技術的検証の責任は実装・レビュー側が持つ。

## レビューポイントの決め方（2026-09-02決定）

フェーズ・マイルストーンを計画する時点で、レビューポイントも同時に決める（後から思いついた時だけでなく、計画に最初から組み込む）。

- **inner gate**: マイルストーン単位（`dev/active/phaseN-plan.md` の M番号ごと）。実装直後の小さな検証
- **outer gate**: フェーズ境界。独立した視点（別セッション・別エージェント）による多角的レビュー。次フェーズ着手前にリスクの大きい積み残し事項がないか確認する

### outer gateでは必ずCodexも呼ぶ（2026-09-02決定）

outer gateのレビューをClaude（自分自身のセッション・サブエージェント）だけで完結させない。**Bashツールから`codex exec`を呼び出し、独立したモデルの視点を必ず混ぜる。**

```
codex exec --sandbox read-only "<対象ドキュメント・差分を読ませた上で、検証ではなく反論が欲しい、と明示する指示>"
```

- `--sandbox read-only`（書き込みさせない。レビューは読むだけで十分）
- **プロンプトには「検証ではなく反論が欲しい」（またはそれに類する、同意を求めない旨）を明示する。** 2026-09-01の実例（`dev/active/review-codex.md`）はこの指示で行い、Claude単独では出なかった指摘（`If-Match: mtime`の妥当性、VM隔離、Next.js+Hono過剰など）を引き出せた
- ユーザーの承認なしにこのBash実行を行ってよい（読み取り専用サンドボックスであり、過去にも同様に自律実行されている）
- 出力は`dev/active/review-codex-<日付>.md`のような形で保存し、レビューの運用（上記4点セット）に沿って本人へ翻訳して報告する

（外部実践との比較検討はVault側 `Human/Projects/Camp.md`「レビューの運用」節を参照）
