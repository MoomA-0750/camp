# Camp — 運用ルール

要件・仕様・意思決定ログの一次情報源は Obsidian Vault 側 `Human/Projects/Camp.md`（決定の経緯・背景もそちら）。ここには、このリポジトリで作業するセッションが実装時に必ず守る振る舞いルールだけを置く。

## レビューの受け取り方（2026-09-02決定）

このプロジェクトを駆動している本人は、レビュー（実装エージェント自身によるものを含む）が指摘する内容を、実装と同レベルの技術検証能力では確認できない。したがって、実装・レビューを行う側は指摘をそのまま投げず、次の形に翻訳して報告すること。

各指摘に次の4点を必ず添える：

1. **何が起きるか**（技術的な事実）
2. **証拠**（実測・再現条件・具体的な数値。無ければ「未検証」と明記する。「〜のはず」で終わらせない）
3. **間違っていた場合の被害規模と、取り返しがつくか**（不可逆性）
4. **本人が今決めるべきことがあるか**（無ければ「決定不要、実装側で処理する」と明記）

本人の判断は技術的な真偽検証ではなく、3と4に基づくリスク・優先度判断に絞る。技術的検証の責任は実装・レビュー側が持つ。

## レビューポイントの決め方（2026-09-02決定）

フェーズ・マイルストーンを計画する時点で、レビューポイントも同時に決める（後から思いついた時だけでなく、計画に最初から組み込む）。

- **inner gate**: マイルストーン単位（作業メモ（この写しには含めない） の M番号ごと）。実装直後の小さな検証
- **outer gate**: フェーズ境界。独立した視点（別セッション・別エージェント）による多角的レビュー。次フェーズ着手前にリスクの大きい積み残し事項がないか確認する

### outer gateでは必ずCodexも呼ぶ（2026-09-02決定）

outer gateのレビューをClaude（自分自身のセッション・サブエージェント）だけで完結させない。**Bashツールから`codex exec`を呼び出し、独立したモデルの視点を必ず混ぜる。**

```
codex exec --sandbox read-only "<対象ドキュメント・差分を読ませた上で、検証ではなく反論が欲しい、と明示する指示>"
```

- `--sandbox read-only`（書き込みさせない。レビューは読むだけで十分）
- **プロンプトには「検証ではなく反論が欲しい」（またはそれに類する、同意を求めない旨）を明示する。** 2026-09-01の実例（作業メモ（この写しには含めない））はこの指示で行い、Claude単独では出なかった指摘（`If-Match: mtime`の妥当性、VM隔離、Next.js+Hono過剰など）を引き出せた
- ユーザーの承認なしにこのBash実行を行ってよい（読み取り専用サンドボックスであり、過去にも同様に自律実行されている）
- 出力は作業メモ（この写しには含めない）のような形で保存し、レビューの運用（上記4点セット）に沿って本人へ翻訳して報告する

### Fable審査点を計画時に決める（2026-09-11決定）

Fableは消費が大きいので、毎回は呼ばない。フェーズ計画を書くとき、各マイルストーンを次の4条件で判定し、1つでも当たるものを「Fable審査点」として作業メモ（この写しには含めない）に理由付きで書く。**差分の大きさでは決めない。**

- **やり直せない**: DBスキーマ、データの移行・削除、公開リポジトリへのpush
- **安全境界に触る**: 認証・再認証、cwd・SSH接続先の許可リスト、監査ログ、`camp`ユーザーの分離、秘密情報
- **後の工程が依存する**: 間違えると後続のマイルストーンがやり直しになる土台（形式、プロトコル、スキーマ）
- **テストで拾いにくい**: 並行処理、プロセスの寿命、タイミング依存

```markdown
## Fable審査点
- M32 設計レビュー — ビュー定義の形式（以後.baseを読まない土台。やり直しが大きい）
```

- 呼び方: `fable-reviewer`サブエージェント（`~/.claude/agents/fable-reviewer.md`）をAgentツールで呼ぶ。セッションの既定モデルは変えない。`/model fable`は既定を書き換え、`campd agent`が`--model`なしで起こすセッションまでFableになるため
- 見せるのは原則として**実装前の設計**。実装後に見せるときは、審査点に当たるファイル・差分に絞る
- 審査点の一覧は計画を確定するときに本人へ見せる。計画で了承された審査点では、確認なしで呼んでよい
- **計画にない呼び出し**は、次のどれかに当たったときだけ、理由を一行添えて本人に確認してから行う
  - 計画にない形で安全境界に触ることが分かった
  - 設計が計画から変わった
  - Opusのレビュー役同士で意見が割れた
  - 同じ不具合が2回戻ってきた
- 目安はフェーズあたり2〜3回。outer gateで`/usage`の週次消費を見て、本人と回数を調整する
- FableもClaude系列なので、outer gateのCodexの反論の代わりにはしない
- 出力は作業メモ（この写しには含めない）に保存し、上記4点セットで本人へ報告する

（外部実践との比較検討はVault側 `Human/Projects/Camp.md`「レビューの運用」節を参照）

## Camp の立ち位置（2026-09-12決定）

実装で守る振る舞い。経緯は Vault の `Human/Projects/Camp.md`、決定は `docs/10-decisions.md` の D-030・D-031。

- **Camp は CLI の忠実なリモコン。門番にしない。** エージェントは CLI で開いたときと同じ置き場・設定・ルール・
  フック・プラグインで動かす。Camp は止める・見せる・記録するに徹する。**CLI と違う動きにする守りを足そうと
  したら、止まって本人に確かめる**
- 緩めない最低限: tailnet の中だけ（funnel なし）／Camp 自身のログインと、別のサイトから操られない守り
  （SameSite・Origin）／ノートや AI の出力を HTML としてそのまま描かない
- **エージェントは対等。** 機能は全エージェントに入れるか、どれにも入れない。エージェント固有の手順は駆動器に
  閉じ、実行面の本流・campd・API・画面にエージェント名を決め打ちしない。エージェント固有の振る舞いは隠さず
  共通の欄で見せる

## 公開リポジトリの更新（2026-09-11決定）

https://github.com/MoomA-0750/camp は、このリポジトリの**写し**。このリポジトリには remote を付けない。

- 更新はフェーズの区切り（outer gate のあと）に行う。作業コピーは `~/Documents/git-cloned/camp-public/`、同期は 作業メモ（この写しには含めない）
- `dev/` は出さない。Vault の実データ（ノート名・会話の題名・秘密の在り処）は置き換える。置き換えの規則は同期スクリプトが持ち、規則が当たらなくなったら止まる
- 同期のあと 作業メモ（この写しには含めない） で写しの履歴全体を見て、残りを読んで確かめてから commit する
- **push の前に本人に確認する**

## 既知の秘密を扱うときのルール（2026-09-03決定）

秘密が「どこに何件写っているか」を数える作業では、**値と、値を含む行を、
一度も出力に出さない**。2026-09-03 にこれを破って会話の記録に値を出した。
原因は、走査そのものではなく、**照合語を取り出すために書いた場当たりの差分表示**
（前後の文脈を印字したため伏字化から漏れた）。

- 数えるときは 作業メモ（この写しには含めない） だけを使う。標準入力から値を受け取り、
  出力は件数と場所だけ
- 照合語を git 履歴などから取り出す処理も、**その場で書かずにスクリプトへ入れる**。
  「今回だけ」の一行コマンドで秘密に触らない
- 伏字化するときは前後の文脈も伏せる。長さと `sha256` の先頭数桁までが上限
- 破ってしまった場合は、隠さず報告し、**露出面の表を更新して、その値についての
  既存の判断を再検討にかける**。ただし本人が「再検討しない」と決めた値については、
  露出が増えても**再提起しない**（`Vault のあるノート` 由来の8文字が該当。
  2026-09-03決定）。記録は続けるが、判断は蒸し返さない
